using UnityEngine;
using UniRx;
public class Debug_InputManager : MonoBehaviour
{
    void Start()
    {
        /*InputManager.Instance.LeftCrossKey.Subscribe(x =>
        {
            Debug.Log($"LeftCrossKey :{x}");
        }).AddTo(this);
        InputManager.Instance.Cancel.Subscribe(x =>
        {
            Debug.Log($"Cancel :{x}");
        }).AddTo(this);
        InputManager.Instance.Decision.Subscribe(x =>
        {
            Debug.Log($"Decision :{x}");
        }).AddTo(this);*/
    }
}
